
<section class="jumbotron text-center">
  <div class="container">

    <div class="row">
      <div class="col-md-4 mx-auto mb-5">
        <img class="img-fluid single-cover artist-cover" src="https://via.placeholder.com/348x225/54595b/ffffff/?text=Thumbnail" alt="{{ArtistName}}">
      </div>
    </div>

    <h1 class="jumbotron-heading">{{ArtistName}}</h1>
    <p class="lead text-muted biography" data-artist="{{ArtistName}}">Something short and leading about the collection below—its contents, the creator, etc. Make it short and sweet, but not too short so folks don't simply skip over it entirely.</p>
  </div>
</section>

<div class="container">
  <div class="row">
    <div class="col">
      <table class="table">
        <thead class="thead-dark">
          <tr>
            <th class="d-none d-md-table-cell" scope="col">#</th>
            <th scope="col">Track</th>
            <th class="d-none d-md-table-cell" scope="col">Album</th>
            <th class="d-none d-md-table-cell" scope="col">Genre</th>
            <th scope="col">Price</th>
          </tr>
        </thead>
        <tbody>
          {{tracks.SequelizeInstance}}
          {{# each tracks}}
          <tr>
            <th class="d-none d-md-table-cell" scope="row">1</th>
            <td>{{Track}}</td>
            <td class="d-none d-md-table-cell">{{Album}}</td>
            <td class="d-none d-md-table-cell">{{Genre}}</td>
            <td scope="price">{{Price}}</td>
          </tr>
          {{/each}}
        </tbody>
      </table>
    </div>
  </div>
</div>


<div class="album p-3 p-md-5 bg-light">
  <div class="container-fluid">

    <div class="row artist-albums mx-auto text-center"></div>
  </div>
</div>